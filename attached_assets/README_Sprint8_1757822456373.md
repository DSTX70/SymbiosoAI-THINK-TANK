# SymbiosoAi — Sprint 8 Handoff Package
Date: 2025-09-14

**Theme:** Scale & Hardening — performance, resilience, tenant isolation, reliability ops, SOC2 gap prep

## Scope (what to wire)
1) **Performance & Resilience**
   - Circuit Breaker middleware (with half‑open) and Retry helper for external calls.
   - LLM Response Cache (TTL, key = prompt+mode+policy).
   - Rate Limit presets for public routes; error taxonomy and standard error envelopes.

2) **Tenant Isolation (end‑to‑end)**
   - Org header enforcement (reuse from S6) on *all* routes + export signing checks.
   - DB: RLS policy verification + new indexes for hot queries.

3) **Reliability Ops**
   - `/ops/health` + `/ops/echo` endpoints (uptime, queue depth placeholders).
   - Backup/Restore checklist (docs) + runbook stubs.
   - Alert thresholds & dashboards (p95/p99 latency, error_rate, queue backlog).

4) **SOC2 Gap Prep**
   - Security headers/CSP template; logging redaction; access review checklist.

## Wire points
- Mount ops routes (`/ops/health`, `/ops/echo`).
- Apply DB migration `20250913_sprint8.sql`.
- Add circuitBreaker + rateLimiter to app middleware chain (before routes).
- Wrap external calls with `withRetry()`; wrap LLM calls with `llmCache.getOrSet()`.

## Owners
- Forge: middleware wiring, indexes, metrics.
- LexiCode: route guards and header enforcement, export signing checks.
- CodeBlock/Lume: error states/skeletons, cache hit indicators (optional).
- Sentinel: CSP profile, log redaction, SOC2 gap checklist alignment.
- Navi: SLO thresholds and alert receivers; drill schedule (backup/restore).
