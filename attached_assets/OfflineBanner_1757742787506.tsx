import React from 'react'; export default function OfflineBanner(){const [o,setO]=React.useState(!navigator.onLine); React.useEffect(()=>{const on=()=>setO(false),off=()=>setO(true); window.addEventListener('online',on); window.addEventListener('offline',off); return()=>{window.removeEventListener('online',on); window.removeEventListener('offline',off);};},[]); if(!o) return null; return <div data-testid='offline-banner'>Offline</div>; }